import 'package:flutter/material.dart';

class AppColors {
  // Vibrant, futuristic Electric Blue / Neon Blue
  static const Color cobaltBlue = Color.fromARGB(255, 0, 170, 255); 

  // Dark Mode Colors
  static const Color trueBlack = Color.fromARGB(255, 0, 14, 35);
  static const Color darkSurface = Color.fromARGB(255, 40, 40, 40); // Slightly elevated from black

  // Light Mode Colors
  static const Color pureWhite = Color.fromARGB(255, 231, 224, 224);
  static const Color lightSurface = Color.fromARGB(255, 221, 223, 226); // Very faint light grey tint
}
