import 'package:flutter/material.dart';

class AppColors {
  // Vibrant, futuristic Electric Blue / Neon Blue
  static const Color cobaltBlue = Color.fromARGB(255, 0, 170, 255); 

  // Dark Mode Colors
  static const Color trueBlack = Color(0xFF000000);
  static const Color darkSurface = Color.fromARGB(255, 40, 40, 40); // Slightly elevated from black

  // Light Mode Colors
  static const Color pureWhite = Color(0xFFFFFFFF);
  static const Color lightSurface = Color(0xFFF8F9FA); // Very faint light grey tint
}
